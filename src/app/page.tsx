import MainLayout from '@/components/MainLayout';

export const metadata = {
  title: 'AnswerAssistant | Smart Application Reuse',
  description: 'Reuse your best program application answers with semantic search power.',
};

export default function Home() {
  return <MainLayout />;
}
